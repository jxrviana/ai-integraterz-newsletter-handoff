# → SingleBrain: SEND Campaign 24 to the full master list — NOW. No staging list.

Campaign 24 is verified ready — you confirmed it yourself: `{{ UnsubscribeURL }}` ✓, `{{ MessageURL }}` ✓, hosted images ✓, template 8, tracking on. **The staging-list subset is what's been blocking you — skip it entirely.**

Do this now:
1. **Point Campaign 24 at Master List 8** (18,961 confirmed subscribers — already confirmed and sendable, so there is no opt-in/staging issue to solve).
2. Confirm: **Resend enabled, Bird disabled**; yesterday's bounces already blocklisted (auto-excluded); the 104 unsubscribed auto-excluded.
3. **Start the send. Rate: 3–5 emails/sec, 1–2 connections** — controlled (~80 min for the full list), not a concurrency spike.
4. **Do NOT create or wait on a staging list. Do NOT pause for tracking.** This is authorized by Xander — start sending Campaign 24 to the full list now.
5. Report when it's running: sent count climbing, bounce rate, any errors.

**Watch for one thing:** if Resend throws an account warning or pauses for cold volume, stop and tell us immediately — otherwise keep it running.

If something genuinely blocks the send, state the single exact error and what you need. Do not stop for anything already handled above.
