# → SingleBrain: adopt the full updated Newsletter Skills Library

*(Xander: paste this together with all the skill files from the `skills/` folder. This gets you and Claude working from identical context.)*

Here is our complete, current skills library — the same context Claude works from, so both sides stay in sync. **Save/UPDATE your skills from these files. Where you already have a skill by a similar name, REPLACE it with ours — ours are current.**

## General skills (reusable across every newsletter) — `skills/general/`
- `00-pipeline` — the end-to-end workflow
- `01-creation-and-QA` — the rules + the mandatory QA checklist (you already saved this as `newsletter-creation-qa-guide`; this is the same standard — keep it current to this)
- `02-format-playbook` — editorial card structure
- `03-offer-affiliate-program` — the offer
- `04-design-front-desk-template` — the LOCKED design (base for all niches)
- `05-images` — image-prompt rules (you never generate images)
- `06-sending-listmonk` — ListMonk, SMTP, tracking, bounce, deliverability

## Newsletter identity skills (design-locked only) — `skills/newsletters/`
- `the-dealmaker` — **UPDATE** your existing one to this
- `the-referral-channel` — **UPDATE** your existing one to this
- `front-desk` — **NEW** (defines the locked template)
- `local-affiliate-pro` — **UPDATE** your `affiliate-deals-roundups` to match this

## Cleanup
- **DELETE / deprecate the `the-rainmaker` skill** — it's a stale duplicate of `the-dealmaker` (the newsletter was renamed). Use `the-dealmaker` only.

## Not yet provided (design not finalized)
No identity skills yet for **Agency Insider, The Protocol, The Green Room, The People Person, The Daily Sun.** Keep using the general skills for their shared rules; we'll send each identity skill once its design is locked. **Do NOT invent their designs.**

## Standing reminders baked into all of these
One newsletter at a time · browser-verify every link · never fabricate · no "AI Integraterz" / "Published by AI Integraterz" · never "power partner" · CTA = `https://whimsy-nebula-47ch.here.now/` · run the QA checklist + attach the report before every delivery · never auto-send (Xander approves).

**Confirm which skills you updated, replaced, and deleted.**
