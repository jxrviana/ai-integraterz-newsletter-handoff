# SPEC 2 — THE FUTURE TODAY CLONE

Faithfully clone the design of **The Future Today / Forward Future** (a premium daily). Read SHARED.md for rules, branding, and content. Save to:
`C:\Users\Xander\Claude\Cowork\AI Integraterz\Newsletter\design-directions\v2\html-2-future-today.html`

The Future Today look: **big deep-navy headlines, small ORANGE uppercase category labels, white cards with thin borders, lots of whitespace, real photos, a light-grey footer.** More editorial and spacious than The Rundown. NO emojis.

## Palette
- Page background `#ffffff`; card background `#ffffff`
- Card border `1px solid #e6e6e6`, border-radius `6px`, padding `26px`, `18px` gap between cards
- Masthead / footer-social near-black `#0d0d0d`
- Accent (category labels + logo tile) ORANGE `#ef5b2b`
- Headlines DEEP NAVY `#14233d`, bold, LARGE
- Body `#2f3033`; muted `#6b6b70`; links `#2563a8` blue
- Footer background `#f4f4f5`

## Type
`Arial, 'Helvetica Neue', Helvetica, sans-serif`. Body `15px`/`1.65`. **Headlines `26px` navy bold** (top-story headline `30px`). Category labels `11px` ORANGE UPPERCASE letter-spacing `1.5px` bold. Mini sub-headlines `17px` navy bold.

## Layout (600px wide, centered)
1. **Top row:** left `January 15, 2026` (`12px` `#6b6b70`); right `Read online` (`12px` blue).
2. **Masthead:** near-black block (square corners), padding `36px`, centered white wordmark **THE DISPATCH** (`28px` bold) with an orange `#ef5b2b` rounded-square tile (white "D") immediately before the wordmark (mimics the orange F).
3. **Intro card:** `Good morning. It's Thursday, January 15,` bold + greeting text; blank line; `Plus: today's prompt on uncovering your invisible work.`
4. **Top Stories card:** orange label `YOUR DAILY ROLLUP` → large navy headline `Top Stories of the Day` → image placeholder → then THREE mini-stories, each = bold navy underlined sub-headline + a paragraph. Use Story A, Story B, and a third short one: sub-headline `A Team's Counterintuitive Win` + paragraph "One team grew output without growing headcount — by removing work, not adding tools."
5. **Story card:** orange label `TECHNOLOGY` → large navy headline (reuse Story A headline OR a fresh line `The Metric That Predicts Who Wins`) → image → 1–2 body paragraphs → `Continue reading here.` blue link.
6. **Story card (HOUSE / NATIVE — identical style):** orange label `THE DISPATCH` → large navy headline `How THE DISPATCH Gets You There First` → image → body (Story C) → `Continue reading here.` blue link. Byte-identical card styling to the others.
7. **Story card:** orange label `MARKET` → navy headline `The One Number Nobody's Talking About` → image → body (Story B) → link.
8. **What Else card:** orange label `NEWS` → navy headline `What Else Is Happening` → image → disc bullets, bold lead + text (Everything-else content).
9. **The Daily Byte card:** orange label `THE DAILY BYTE` → navy headline `Privacy Is the New Status Symbol` → image → a short lighter-tone paragraph: "The new luxury isn't more features — it's software that quietly protects your attention and your data. Here's why the best teams are designing for less."
10. **Prompt of the Week:** orange label `PROMPT OF THE WEEK` → navy headline `The Invisible-Work Audit` → a light-grey (`#f4f4f5`) bordered box containing the prompt copy (from content), italic or `Georgia` serif for the quote feel.
11. **Ratings card:** `That's All for Today` bold + `Before you go, what did you think of today's issue?` + three stacked bordered boxes (blue text): `Loved it — read every word`, `Liked it — skimmed a bit`, `Needs work`. + `Thanks for reading. See you next time.`
12. **Sign-off:** italic `— The Editor, THE DISPATCH`.
13. **Footer** (`#f4f4f5` bg, centered, `12px`): small social glyph placeholders row → `Stay Connected` bold + `Follow us, subscribe, and catch us anywhere.` → `Partner With Us` bold + `Get in front of tech-forward readers. Reply to this email.` → `Update your email preferences or unsubscribe here.` → `© 2026 AI Integraterz` → `37460 Beacon Brick Road, Zephyrhills, Florida 33541`.

Faithful, spacious, editorial, premium. Big navy headlines, orange labels. NO emojis anywhere.
