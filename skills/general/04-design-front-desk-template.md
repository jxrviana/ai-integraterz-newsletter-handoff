# RETIRED — Design (moved)

The "locked Front Desk template for all niches" this file used to define is **RETIRED** (Aug 4 huddle). Design is now **per-newsletter** — every newsletter is its own publication with its own look.

→ See **`04-design.md`** (per-newsletter design principles) and **`newsletter-types.md`**. Each newsletter's actual design lives in its own skill under `../newsletters/`.
